# Test Environment Configuration
# Database Configuration for tests (connects to local Docker DB)
POSTGRES_DB=events
POSTGRES_USER=events_user
POSTGRES_PASSWORD=events_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Application Configuration
LOG_LEVEL=DEBUG
RATE_LIMIT_PER_MINUTE=10000
DEBUG=true

# JWT Authentication - use test keys
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only-32-chars-min
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Test API URL
API_BASE_URL=http://localhost:8000

# External Ports (for host machine)
APP_EXTERNAL_PORT=8000
